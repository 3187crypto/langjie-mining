{
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "devCommand": "npm start",
  "installCommand": "npm install",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/contracts/(.*)", "dest": "/contracts/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}